#include "player.hpp"

char const PLAYER::getName() {
    return name;
}

int const PLAYER::decide() {
    std::cout << "Error in program, this function should never be used. Check the used class!\n";
    return -1;
}